spring:
  application:
    name: companies-crud
  datasource:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
    password: secret
    url: jdbc:postgresql://localhost:5432/companies
    username: admin